import java.awt.*;import java.awt.event.*;import java.util.*;public class SimpleTablePanel extends Panel {    private boolean DEBUG = true;	public Table table = null;    public SimpleTablePanel(String title, Object[][] data, String[] columnNames ) {        super();        setName(title);        table = new Table(title,data, columnNames);       // table.setPreferredScrollableViewportSize(new Dimension(500, 70));/*        if (DEBUG) {            table.addMouseListener(new MouseAdapter() {                public void mouseClicked(MouseEvent e) {                    printDebugData(table);                }            });        }*/        //Create the scroll pane and add the table to it.         ScrollPane scrollPane = new ScrollPane();        scrollPane.add(table);        //Add the scroll pane to this window.        add(scrollPane, BorderLayout.CENTER);    }        public SimpleTablePanel(String title, Vector theData,Vector theColumnNames) {        super();        setName(title);                Vector data = theData;        Vector columnNames = theColumnNames;       table = new Table(title, data, columnNames);       //Create the scroll pane and add the table to it.         ScrollPane scrollPane = new ScrollPane();        scrollPane.add(table);        //Add the scroll pane to this window.        add(scrollPane, BorderLayout.CENTER);    }        public void setData(Vector data, Vector columnNames) {    	table.setData(data,columnNames);    }        private static void printDebugData(Table table) {        int numRows = table.getRowCount();        int numCols = table.getColumnCount();      //  javax.swing.table.TableModel model = table.getModel();        System.out.println("Value of data: ");        for (int i=0; i < numRows; i++) {            System.out.println("    row " + i + ":");            for (int j=0; j < numCols; j++) {           //     System.out.println("  " + model.getValueAt(i, j));            }            System.out.println("");        }        System.out.println("--------------------------");    }}
